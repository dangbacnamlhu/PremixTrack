# Server
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Database - SQLite (mặc định, chạy ngay không cần cài DB)
DATABASE_URL="file:./dev.db"

# Nếu dùng PostgreSQL (uncomment và sửa):
# DATABASE_URL="postgresql://user:password@localhost:5432/premixtrack?schema=public"
